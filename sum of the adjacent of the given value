#include <stdio.h>

int main() {
    int i,j,n,k,l=0,m,c,r;
    scanf("%d %d %d",&r,&c,&n);
    int a[r][r];
    char b[100]={0};
    //printf("%d ",b[0]);
    for(i=0;i<r;i++){
        for(j=0;j<r;j++){
            scanf("%d",&a[i][j]);
        }
    }
    for(i=0;i<r;i++){
        for(j=0;j<r;j++){
            if(i==c-1&&j==c)
            l=l+a[i][j];
            else if(i==c+1&&j==c)
            l=l+a[i][j];
            else if(j==c+1&&i==c)
            l=l+a[i][j];
            else if(j==c-1&&i==c)
            l=l+a[i][j];
            
        }
    }
    printf("%d",l);
}
    
