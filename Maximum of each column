#include <stdio.h>

int main()
{
   int row,col,max=0,r;
   scanf("%d %d",&row,&col);
   int a[row][col];
   
   for(int i=0;i<row;i++){
       for(int j=0;j<col;j++){
           scanf("%d",&a[i][j]);
       }
   }
   for(int i=0;i<row;i++){
      for(int j=0;j<col;j++){
           max=a[j][j+1];
           if(max<a[j][i])
           max=a[j][i];
           
        }
        printf("%d",max);
       printf("\n");
       
       
   }
  

    return 0;
}
